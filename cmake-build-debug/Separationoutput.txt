分隔符识别如下：
(separation,#)
(separation,;)
(separation,()
(separation,,)
(separation,))
(separation,{)
(separation,})
(separation,")
分隔符识别如下：
(separation,#)
(separation,;)
(separation,()
(separation,,)
(separation,))
(separation,{)
(separation,})
(separation,")
